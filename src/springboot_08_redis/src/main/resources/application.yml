# 用之前先启动redis
spring:
  redis:
    # 默认
    host: localhost
    port: 6379
    # 默认 lettuce 线程安全， jedis 线程不安全
    client-type: jedis
    lettuce:
      pool:
        max-active: 8
    jedis:
      pool:
        max-active: 8
